#include<stdio.h>

int main()
{
	float speed = 40;
	float time = 1.5;
	float distance;

	distance = speed * time;
	printf("The distance between company and home is %f km.\n", distance);

	return 0;
}
